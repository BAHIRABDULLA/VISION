

const monthlyKeys = ['4 one-hour sessions per month','Direct message access','Code review support']

const singleKeys = ['1 one-hour session','Session recording','Follow-up notes']

export {monthlyKeys,singleKeys}